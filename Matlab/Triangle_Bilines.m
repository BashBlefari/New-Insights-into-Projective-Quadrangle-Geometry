% bilines of Triangle the tool_midpoint function can be called to find the
% bilines as the tool does not specify whether column or row vector.

[A1,B1,a1,b1]=tool_midpoint(T2,T3,C);
[A2,B2,a2,b2]=tool_midpoint(T1,T3,C);
[A3,B3,a3,b3]=tool_midpoint(T1,T2,C);